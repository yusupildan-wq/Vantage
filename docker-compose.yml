services:
  vantage:
    build: .
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - backend/.env
    environment:
      HOST: 0.0.0.0
    volumes:
      - vantage-data:/app/data
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

volumes:
  vantage-data:
